version: "3.8"

services:
  scraper:
    build: .
    container_name: scraper-dev
    ports:
      - "8000:8000"
    volumes:
      - .:/app        # Mount your local folder into the container
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
